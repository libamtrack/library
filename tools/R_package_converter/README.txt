In order to run package converter you have to:

1. Copy (or make a link) "include" directory containing all libamtrack header files
into this directory

2. Run "read.header.R" script

3. Run "convert.header.to.Rwrapper.R" script